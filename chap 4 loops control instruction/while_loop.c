#include<stdio.h>

int main(){
    int i = 0;
    while (i<4) // if i is less than 4 then the code runs
    {
        printf("happy birthday\n");
        i = i + 1;//the block would run as long as the conditionn is true
        //i++; incrementing operators
    }
    
    return 0;
}