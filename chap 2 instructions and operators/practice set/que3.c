//Write a program to check whether a number is divisible by 97 or not.

#include<stdio.h>

int main(){

    int a =485;

    printf("The value of a%97 is %d\n", a%97);
    printf("If the value comes 0 then a is divisible by 97 if it doesnt come 0 then its not divisible\n");


    return 0;
}